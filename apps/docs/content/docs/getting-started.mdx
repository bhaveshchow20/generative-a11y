---
title: "Getting started"
description:
  "Install generative-a11y and add paced screen-reader announcements to
  streaming AI responses and agent tools with framework-independent TypeScript packages."
publicPath: "/docs/getting-started"
section: "Getting started"
keywords: ["install", "quick start", "framework neutral", "TypeScript"]
related: []
order: 0
---

export const sourceMetadata = {
  publicPath: "/docs/getting-started",
  section: "Getting started",
  related: [],
  order: 0,
};

export const searchableText = [
  "Getting started",
  "Install @generative-a11y/react and @generative-a11y/ai-sdk for an existing React + AI SDK app.",
  "Requires Node.js 22+, ai 7.0.x, @ai-sdk/react 4.0.x, React and React DOM 18.2 or 19, and compatible zod.",
  "Provider-owned runtime and browser delivery; compose callbacks before useChat and observe public state.",
  "Preserve transport, host options, semantic structure, keyboard controls and accessible content.",
  "Advanced custom browser integration: install @generative-a11y/core and @generative-a11y/dom; bindRuntime and dispose delivery before runtime.",
  "Choose an integration: React, AI SDK, assistant-ui, AG-UI / CopilotKit, devtools.",
  "Retry detection unavailable; DOM delivery is not screen-reader output."
].join("\n");

<span id="install"></span>

## Start with your existing React + AI SDK app

```shell
npm install @generative-a11y/react @generative-a11y/ai-sdk
```

Your existing AI SDK setup must also satisfy its `zod` peer
(`^3.25.76 || ^4.1.8` for the checked SDK version).

Requires Node.js 22+, `ai@7.0.x`, `@ai-sdk/react@4.0.x`, and compatible React /
React DOM peers (`^18.2.0 || ^19.0.0`). These are existing app prerequisites;
core and DOM install transitively. Declare a package directly if you import it.

Follow the [React + AI SDK integration](/docs/integrations/ai-sdk):

1. Wrap your chat once with `A11yProvider` to own runtime, browser delivery, and cleanup.
2. Create accessibility callbacks before `useChat`, preserving your transport and existing callbacks.
3. Observe the returned public state and keep your existing message list and composer.

This adds paced live-region updates for supported lifecycle events without
changing the visual UI or moving focus during ordinary streaming. Your host
still owns semantic structure, keyboard controls, and accessible content.
Retry detection is unavailable in the AI SDK adapter. Browser delivery does not
prove what a real screen reader speaks.

For React without AI SDK, install `@generative-a11y/react` and supply confirmed
lifecycle events from your app. See the [React API](/api/react).
For other frameworks, [choose an integration](#choose-an-integration).

## Advanced: custom browser integration

Use core and DOM directly when your app owns lifecycle reporting:

```shell
npm install @generative-a11y/core @generative-a11y/dom
```

<span id="minimal&#45;integration"></span>

## Minimal integration

Create one runtime and connect it to the browser. Send only the new text from
each streaming update. Report when the response finishes, fails, or stops.
Dispose both resources when the session ends.

{/* prettier-ignore */}
```ts
import { createRuntime } from "@generative-a11y/core";
import { bindRuntime } from "@generative-a11y/dom";

const runtime = createRuntime();
const delivery = bindRuntime(runtime);

// Send these events from your existing response lifecycle callbacks.
runtime.dispatch({ type: "response.started", responseId: "response-1" });
runtime.dispatch({
  type: "response.text.delta",
  responseId: "response-1",
  delta: "A complete sentence.",
});
runtime.dispatch({ type: "response.completed", responseId: "response-1" });

// Call this when the chat is removed, not when a response finishes.
export function disposeChat() {
  delivery.dispose();
  runtime.dispose();
}
```

### Walkthrough

<Steps>

<Step>

### Create one runtime

Your runtime tracks responses, prepares useful text, and controls update timing.

</Step>

<Step>

### Connect the browser

bindRuntime creates and manages the hidden live regions used for
screen&#45;reader updates.

</Step>

<Step>

### Report app events

Tell the runtime when a response starts, receives text, and finishes.

</Step>

<Step>

### Clean up

Dispose the browser connection first, then dispose the runtime when the session
ends.

</Step>

</Steps>

<Callout title="Evidence and testing note">

generative&#45;a11y can confirm when it adds an announcement to the page. Test
with real screen readers to confirm what people hear.

</Callout>

<span id="choose&#45;an&#45;integration"></span>

## Choose the package that matches your app

Use a framework adapter when your app already uses that framework. For a custom
app, send events directly to the core runtime.

<table aria-label="Choose the package that matches your app table">
  <thead>
    <tr>
      <th>Application</th>
      <th>Package</th>
      <th>What it reports</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Framework&#45;neutral</td>
      <td>@generative&#45;a11y/core &#43; /dom</td>
      <td>All standard library events</td>
    </tr>
    <tr>
      <td>React</td>
      <td>@generative&#45;a11y/react</td>
      <td>Provider, delivery, attention, preferences</td>
    </tr>
    <tr>
      <td>AI SDK useChat</td>
      <td>@generative&#45;a11y/react + @generative&#45;a11y/ai&#45;sdk</td>
      <td>Streaming, terminals, tools, approvals, citations</td>
    </tr>
    <tr>
      <td>assistant&#45;ui</td>
      <td>@generative&#45;a11y/assistant&#45;ui</td>
      <td>Messages, tools, approvals, sources</td>
    </tr>
    <tr>
      <td>AG&#45;UI / CopilotKit v2</td>
      <td>@generative&#45;a11y/ag&#45;ui</td>
      <td>Protocol text, tools, interrupts</td>
    </tr>
    <tr>
      <td>Development diagnostics</td>
      <td>@generative&#45;a11y/devtools</td>
      <td>Redacted runtime and DOM delivery traces</td>
    </tr>
    <tr>
      <td>Deterministic tests</td>
      <td>@generative&#45;a11y/core/testing</td>
      <td>Event recording, replay, and semantic assertions</td>
    </tr>
  </tbody>
</table>
