---
title: "React hooks"
description:
  "React hooks for the runtime, attention, preferences, and refs for existing
  app elements."
publicPath: "/api/react/hooks"
section: "React"
keywords:
  [
    "useA11y",
    "useRuntime",
    "useAttentionRefs",
    "hooks",
  ]
related: []
order: 15
---

export const sourceMetadata = {
  publicPath: "/api/react/hooks",
  section: "React",
  related: [],
  order: 15,
};

export const searchableText = [
  "Attention policy attentionPolicy useAttentionControl bindAttention quiet override /docs/attention-aware-announcements",
  "React hooks",
  "React hooks for the runtime, attention, preferences, and refs for existing app elements.",
  "React",
  "useA11y",
  "useRuntime",
  "useAttentionRefs",
  "hooks",
  "complete-example",
  "Bind an existing chat interface",
  "AttentionRefs contain refs only. Add them to semantic elements already in your app.",
  "tsx",
  'function ExistingChat() { const runtime = useRuntime(); const attention = useAttention(); const { preferences, setPreferences } = usePreferences(); const bindings = useAttentionRefs(); return ( <main ref={bindings.conversationRef}> <article ref={bindings.newestResponseRef}>Latest response</article> <textarea ref={bindings.composerRef} /> <button onClick={() => runtime.dispatch(startEvent)}>Send</button> <output>{attention.mode}</output> <button onClick={() => setPreferences({ ...preferences, streaming: "less" })}> Reduce streaming detail </button> </main> ); }',
  "Read the app runtime",
  "Send only events that your app knows happened.",
  "Register existing elements",
  "Ref bindings track focus and visibility without changing markup or styles.",
  "Expose user control",
  "Preference updates remain validated and versioned.",
  "useA11y()",
  "A11yContextValue",
  "Hook",
  "n/a",
  "Returns runtime, attentionStore, and preferenceStore from the nearest provider.",
  "useRuntime()",
  "Runtime",
  "Hook",
  "n/a",
  "Returns the provider runtime.",
  "useAttention()",
  "AttentionSnapshot",
  "Hook",
  "unknown SSR snapshot",
  "Uses useSyncExternalStore to report browser focus and visibility without guessing missing values.",
  "usePreferences()",
  "PreferencesResult",
  "Hook",
  "n/a",
  "Returns validated preferences, setPreferences, and the underlying store.",
  "useAttentionRefs()",
  "AttentionRefs",
  "Hook",
  "n/a",
  "Returns stable callback refs for the composer, conversation, and newest response.",
  "binding-contracts",
  "AttentionRefs",
  "Each property is a callback ref for an existing host element. These optional refs observe attention only; they do not add semantics or enable announcement delivery. Compose existing refs with your framework utility when necessary.",
  "Property",
  "Target",
  "Purpose",
  "composerRef",
  "HTMLElement",
  "Detects focus in the composer",
  "conversationRef",
  "HTMLElement",
  "Detects focus in conversation history",
  "newestResponseRef",
  "HTMLElement",
  "Checks whether the newest response is visible",
].join("\n");

<span id="complete&#45;example"></span>

## Bind an existing chat interface

AttentionRefs contain refs only. Add them to semantic elements already in your app.

{/* prettier-ignore */}
```tsx
function ExistingChat() {
  const runtime = useRuntime();
  const attention = useAttention();
  const { preferences, setPreferences } = usePreferences();
  const bindings = useAttentionRefs();

  return (
    <main ref={bindings.conversationRef}>
      <article ref={bindings.newestResponseRef}>Latest response</article>
      <textarea ref={bindings.composerRef} />
      <button onClick={() => runtime.dispatch(startEvent)}>Send</button>
      <output>{attention.mode}</output>
      <button onClick={() => setPreferences({ ...preferences, streaming: "less" })}>
        Reduce streaming detail
      </button>
    </main>
  );
}
```

### Walkthrough

<Steps>

<Step>

### Read the app runtime

Send only events that your app knows happened.

</Step>

<Step>

### Register existing elements

Ref bindings track focus and visibility without changing markup or styles.

</Step>

<Step>

### Expose user control

Preference updates remain validated and versioned.

</Step>

</Steps>

<TypeTable
  type={{
    "useA11y()": {
      type: <>A11yContextValue</>,
      description: (
        <>
          Hook. Returns runtime, attentionStore, and preferenceStore from the
          nearest provider.
        </>
      ),
    },
    "useRuntime()": {
      type: <>Runtime</>,
      description: <>Hook. Returns the provider runtime.</>,
    },
    "useAttention()": {
      type: <>AttentionSnapshot</>,
      default: <>unknown SSR snapshot</>,
      description: (
        <>
          Hook. Uses useSyncExternalStore to report browser focus and visibility
          without guessing missing values.
        </>
      ),
    },
    "usePreferences()": {
      type: <>PreferencesResult</>,
      description: (
        <>
          Hook. Returns validated preferences, setPreferences, and the
          underlying store.
        </>
      ),
    },
    "useAttentionRefs()": {
      type: <>AttentionRefs</>,
      description: (
        <>
          Hook. Returns stable composer, conversation, and newest&#45;response
          ref props.
        </>
      ),
    },
  }}
/>

<span id="binding&#45;contracts"></span>

## AttentionRefs

Each property is a callback ref for an existing host element. These optional
refs observe attention only; they do not add semantics or enable announcement
delivery. Compose existing refs with your framework utility when necessary.

<table aria-label="AttentionRefs table">
  <thead>
    <tr>
      <th>Property</th>
      <th>Target</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>composerRef</td>
      <td>HTMLElement</td>
      <td>Detects focus in the composer</td>
    </tr>
    <tr>
      <td>conversationRef</td>
      <td>HTMLElement</td>
      <td>Detects focus in conversation history</td>
    </tr>
    <tr>
      <td>newestResponseRef</td>
      <td>HTMLElement</td>
      <td>Checks whether the newest response is visible</td>
    </tr>
  </tbody>
</table>

## useAttentionControl

Returns `{ state: { observed, override, effective }, setOverride }`. Call `setOverride("quiet")`, `setOverride("normal")`, or `setOverride("auto")` from controls owned by your host app. The provider adds no visible controls. With attention filtering disabled, effective mode remains normal.

Set the provider's `attentionPolicy` prop to bridge browser observations and independently configure core `policy.attention.enabled`. [Copy a complete provider and ref-binding example](/docs/attention-aware-announcements).
