---
title: "React hooks"
description:
  "React hooks for the runtime, attention, preferences, and refs for existing
  app elements."
publicPath: "/api/react/hooks"
section: "React"
keywords: ["useA11y", "useRuntime", "useAttentionRefs", "hooks"]
related: []
order: 15
---

export const sourceMetadata = {
  publicPath: "/api/react/hooks",
  section: "React",
  related: [],
  order: 15,
};

export const searchableText = [
  "React hooks",
  "React hooks for the runtime, attention, preferences, and refs for existing app elements.",
  "Bind an existing chat interface",
  "AttentionRefs contain refs only. Add them to semantic elements already in your app.",
  'import { useAttention, useAttentionRefs, usePreferences, useRuntime, } from "@generative-a11y/react";',
  "function ExistingChat() { const runtime = useRuntime(); const attention = useAttention(); const { setPreferences } = usePreferences(); const bindings = useAttentionRefs();",
  "return (",
  "Latest response",
  'runtime.dispatch({ type: "response.started", responseId: "answer" })}>Send {attention.mode} setPreferences({ version: 1, preset: "balanced", streaming: "completion", tools: "preset" })}> Save completion preference',
  "); }",
  "Mount these hooks under A11yProvider /api/react. Refs observe existing elements without changing their markup or styles; preference updates are validated before the store publishes them. Saving preferences does not change the active runtime policy. Apply them when constructing the next runtime or remounting its provider at an intentional session boundary.",
  "Hook results",
  '"useA11y()" "useRuntime()" "useAttention()" "usePreferences()" "useAttentionRefs()" A11yContextValue',
  "Hook. Returns runtime, attentionStore, and preferenceStore from the nearest provider.",
  "Runtime Hook. Returns the provider runtime. AttentionSnapshot unknown SSR snapshot",
  "Hook. Uses useSyncExternalStore to report browser focus and visibility without guessing missing values.",
  "PreferencesResult",
  "Hook. Returns validated preferences, setPreferences, and the underlying store.",
  "AttentionRefs",
  "Hook. Returns stable composer, conversation, and newest-response ref props.",
  "AttentionRefs",
  "Each property is a callback ref for an existing host element. These optional refs observe attention only; they do not add semantics or enable announcement delivery. Compose existing refs with your framework utility when necessary.",
  "Property Target Purpose",
  "composerRef HTMLElement Detects focus in the composer",
  "conversationRef HTMLElement Detects focus in conversation history",
  "newestResponseRef HTMLElement Checks whether the newest response is visible",
  "useAttentionControl",
  'Returns { state: { observed, override, effective }, setOverride }. Call setOverride("quiet"), setOverride("normal"), or setOverride("auto") from controls owned by your host app. The provider adds no visible controls. With attention filtering disabled, effective mode remains normal.',
  "Set the provider's attentionPolicy prop to bridge browser observations and independently configure core policy.attention.enabled. Copy a complete provider and ref-binding example /docs/attention-aware-announcements.",
].join("\n");

<span id="complete&#45;example"></span>

## Bind an existing chat interface

AttentionRefs contain refs only. Add them to semantic elements already in your
app.

{/* prettier-ignore */}
```tsx title="components/chat.tsx"
import {
  useAttention,
  useAttentionRefs,
  usePreferences,
  useRuntime,
} from "@generative-a11y/react";

function ExistingChat() {
  const runtime = useRuntime();
  const attention = useAttention();
  const { setPreferences } = usePreferences();
  const bindings = useAttentionRefs();

  return (
    <main ref={bindings.conversationRef}>
      <article ref={bindings.newestResponseRef}>Latest response</article>
      <textarea aria-label="Message" ref={bindings.composerRef} />
      <button onClick={() => runtime.dispatch({ type: "response.started", responseId: "answer" })}>Send</button>
      <output>{attention.mode}</output>
      <button onClick={() => setPreferences({ version: 1, preset: "balanced", streaming: "completion", tools: "preset" })}>
        Save completion preference
      </button>
    </main>
  );
}
```

Mount these hooks under [A11yProvider](/api/react). Refs observe existing
elements without changing their markup or styles; preference updates are
validated before the store publishes them. Saving preferences does not change
the active runtime policy. Apply them when constructing the next runtime or
remounting its provider at an intentional session boundary.

## Hook results

<TypeTable
  type={{
    "useA11y()": {
      type: <>A11yContextValue</>,
      description: (
        <>
          Hook. Returns runtime, attentionStore, and preferenceStore from the
          nearest provider.
        </>
      ),
    },
    "useRuntime()": {
      type: <>Runtime</>,
      description: <>Hook. Returns the provider runtime.</>,
    },
    "useAttention()": {
      type: <>AttentionSnapshot</>,
      default: <>unknown SSR snapshot</>,
      description: (
        <>
          Hook. Uses useSyncExternalStore to report browser focus and visibility
          without guessing missing values.
        </>
      ),
    },
    "usePreferences()": {
      type: <>PreferencesResult</>,
      description: (
        <>
          Hook. Returns validated preferences, setPreferences, and the
          underlying store.
        </>
      ),
    },
    "useAttentionRefs()": {
      type: <>AttentionRefs</>,
      description: (
        <>
          Hook. Returns stable composer, conversation, and newest&#45;response
          ref props.
        </>
      ),
    },
  }}
/>

<span id="binding&#45;contracts"></span>

<span id="walkthrough"></span>

<span id="read-the-app-runtime"></span>

<span id="register-existing-elements"></span>

<span id="expose-user-control"></span>

## AttentionRefs

Each property is a callback ref for an existing host element. These optional
refs observe attention only; they do not add semantics or enable announcement
delivery. Compose existing refs with your framework utility when necessary.

<table aria-label="AttentionRefs table">
  <thead>
    <tr>
      <th>Property</th>
      <th>Target</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>composerRef</td>
      <td>HTMLElement</td>
      <td>Detects focus in the composer</td>
    </tr>
    <tr>
      <td>conversationRef</td>
      <td>HTMLElement</td>
      <td>Detects focus in conversation history</td>
    </tr>
    <tr>
      <td>newestResponseRef</td>
      <td>HTMLElement</td>
      <td>Checks whether the newest response is visible</td>
    </tr>
  </tbody>
</table>

## useAttentionControl

Returns `{ state: { observed, override, effective }, setOverride }`. Call
`setOverride("quiet")`, `setOverride("normal")`, or `setOverride("auto")` from
controls owned by your host app. The provider adds no visible controls. With
attention filtering disabled, effective mode remains normal.

Set the provider's `attentionPolicy` prop to bridge browser observations and
independently configure core `policy.attention.enabled`.
[Copy a complete provider and ref-binding example](/docs/attention-aware-announcements).
