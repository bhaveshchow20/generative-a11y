---
title: "@generative-a11y/react"
description:
  "Connect a runtime and browser delivery to your existing React interface."
publicPath: "/api/react"
section: "React"
keywords: ["React", "A11yProvider", "hooks", "bindings"]
related: []
order: 14
---

export const sourceMetadata = {
  publicPath: "/api/react",
  section: "React",
  related: [],
  order: 14,
};

export const searchableText = [
  "@generative-a11y/react",
  "Connect a runtime and browser delivery to your existing React interface.",
  "A11yProvider",
  "A11yProvider can own a runtime or borrow one from your app. It renders stable hidden delivery regions and leaves visible UI unchanged.",
  'import { A11yProvider } from "@generative-a11y/react";',
  "export function App() { return (",
  "); }",
  "Wrap the part of your app that sends events. The provider supplies browser delivery by default; your existing components retain their markup, keyboard behavior, and focus. See React hooks /api/react/hooks to read the runtime.",
  "Provider options",
  "The provider also accepts RuntimeOptions /api/core/create-runtimeoptions when it creates a runtime, including preset, policy, messages, and clock.",
  "runtime delivery attention attentionPolicy attentionStore preferences preferenceStore Runtime owned runtime",
  "Borrows an existing runtime when provided. A11yProvider never disposes app-supplied runtimes.",
  "false | DeliveryOptions {} Configures hidden DOM delivery or disables it. false | AttentionStoreOptions {}",
  "Configures the provider's attention store or disables browser signal collection.",
  "boolean false",
  "Forwards browser observations to core. Enable policy.attention.enabled separately to filter announcements.",
  "AttentionStore owned store Borrows an application-supplied attention store. PreferenceStoreOptions {} Configures an owned preference store. PreferenceStore owned store Borrows an application-supplied preference store.",
  "Provider ownership",
  "A11yProvider cleans up every runtime, browser connection, store, timer, subscription, and live region it creates. Anything supplied by your app remains under your control.",
  "- Provider context is unavailable outside the provider. - Server rendering uses stable hidden structure and reports attention as unknown. - Routine announcements never move focus. - Initial preferences configure an owned runtime only when no explicit preset or policy is supplied. Later preference changes update the store, not the active runtime policy.",
  "Opt-in attention forwarding",
  "A11yProvider accepts attentionPolicy?: boolean (default false). This forwards browser observations to the runtime; independently enable policy.attention.enabled for filtering. useAttentionControl exposes observed, override, and effective state plus setOverride. See the complete attention guide /docs/attention-aware-announcements.",
  "Localized announcements",
  "See catalogs and adapter copy /docs/localized-announcements for the typed messages and copy options, language ownership, validation, construction lifetime, and replay requirements. Existing lifecycle evidence and timing remain unchanged.",
].join("\n");

<span id="provider"></span>

## A11yProvider

A11yProvider can own a runtime or borrow one from your app. It renders stable
hidden delivery regions and leaves visible UI unchanged.

{/* prettier-ignore */}
```tsx title="components/app.tsx"
import { A11yProvider } from "@generative-a11y/react";

export function App() {
  return (
    <A11yProvider>
      <ExistingChat />
    </A11yProvider>
  );
}
```

Wrap the part of your app that sends events. The provider supplies browser
delivery by default; your existing components retain their markup, keyboard
behavior, and focus. See [React hooks](/api/react/hooks) to read the runtime.

## Provider options

The provider also accepts [RuntimeOptions](/api/core/create-runtime#options)
when it creates a runtime, including `preset`, `policy`, `messages`, and
`clock`.

<TypeTable
  type={{
    runtime: {
      type: <>Runtime</>,
      default: <>owned runtime</>,
      description: (
        <>
          Borrows an existing runtime when provided. A11yProvider never disposes
          app&#45;supplied runtimes.
        </>
      ),
    },
    delivery: {
      type: <>false &#124; DeliveryOptions</>,
      default: <>&#123;&#125;</>,
      description: <>Configures hidden DOM delivery or disables it.</>,
    },
    attention: {
      type: <>false &#124; AttentionStoreOptions</>,
      default: <>&#123;&#125;</>,
      description: (
        <>
          Configures the provider's attention store or disables browser signal
          collection.
        </>
      ),
    },
    attentionPolicy: {
      type: <>boolean</>,
      default: <>false</>,
      description: (
        <>
          Forwards browser observations to core. Enable policy.attention.enabled
          separately to filter announcements.
        </>
      ),
    },
    attentionStore: {
      type: <>AttentionStore</>,
      default: <>owned store</>,
      description: <>Borrows an application&#45;supplied attention store.</>,
    },
    preferences: {
      type: <>PreferenceStoreOptions</>,
      default: <>&#123;&#125;</>,
      description: <>Configures an owned preference store.</>,
    },
    preferenceStore: {
      type: <>PreferenceStore</>,
      default: <>owned store</>,
      description: <>Borrows an application&#45;supplied preference store.</>,
    },
  }}
/>

<span id="ownership"></span>

<span id="walkthrough"></span>

<span id="mount-one-provider"></span>

<span id="configure-browser-delivery"></span>

<span id="keep-app-behavior-in-the-app"></span>

## Provider ownership

A11yProvider cleans up every runtime, browser connection, store, timer,
subscription, and live region it creates. Anything supplied by your app remains
under your control.

- Provider context is unavailable outside the provider.
- Server rendering uses stable hidden structure and reports attention as
  unknown.
- Routine announcements never move focus.
- Initial preferences configure an owned runtime only when no explicit preset or
  policy is supplied. Later preference changes update the store, not the active
  runtime policy.

## Opt-in attention forwarding

`A11yProvider` accepts `attentionPolicy?: boolean` (default `false`). This
forwards browser observations to the runtime; independently enable
`policy.attention.enabled` for filtering. `useAttentionControl` exposes
observed, override, and effective state plus `setOverride`.
[See the complete attention guide](/docs/attention-aware-announcements).

## Localized announcements

See [catalogs and adapter copy](/docs/localized-announcements) for the typed
`messages` and `copy` options, language ownership, validation, construction
lifetime, and replay requirements. Existing lifecycle evidence and timing remain
unchanged.

import { ApiReference } from "@/components/api-reference";

<ApiReference entry="@generative-a11y/react" />
