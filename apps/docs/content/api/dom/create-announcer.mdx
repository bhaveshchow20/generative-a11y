---
title: "createAnnouncer"
description:
  "Create an owned browser announcer with progressive ariaNotify delivery,
  stable live-region fallback, diagnostics, and cleanup."
publicPath: "/api/dom/create-announcer"
section: "DOM"
keywords:
  [
    "createAnnouncer",
    "AnnouncerOptions",
    "DeliveryResult",
    "live region",
  ]
related: []
order: 9
---

export const sourceMetadata = {
  publicPath: "/api/dom/create-announcer",
  section: "DOM",
  related: [],
  order: 9,
};

export const searchableText = [
  "createAnnouncer",
  "Create an owned browser announcer with progressive ariaNotify delivery, stable live-region fallback, diagnostics, and cleanup.",
  "DOM",
  "createAnnouncer",
  "AnnouncerOptions",
  "DeliveryResult",
  "live region",
  "signature",
  "Signature and import",
  "auto mode attempts ariaNotify when available, then falls back to the selected polite or assertive live region.",
  "typescript",
  'import { createAnnouncer } from "@generative-a11y/dom"; const announcer = createAnnouncer({ mode: "auto", onDelivery(result) { console.log(result.status, result.method); }, }); const result = announcer.announce(intent); // Call announcer.dispose() when the owning surface is removed.',
  "Use progressive delivery",
  "auto keeps the fallback available when ariaNotify is absent or throws.",
  "Observe the attempt",
  "DeliveryResult reports the browser action without making a speech claim.",
  "Release owned nodes",
  "Dispose removes only live regions created by this announcer.",
  "options",
  "AnnouncerOptions",
  "Optional",
  "{}",
  "Selects document, delivery mode, supplied regions, and diagnostic observation.",
  "return",
  "Announcer",
  "Return",
  "n/a",
  "Owned announcer with announce, getRegions, and dispose methods.",
  "options",
  "AnnouncerOptions",
  "Supplying regions transfers neither node ownership nor removal responsibility to the library.",
  "document",
  "Document",
  "Optional",
  "global document",
  "Document used to create live regions. Without a document, the announcer remains inert and reports unavailable.",
  "mode",
  "auto | live-region",
  "Optional",
  '"auto"',
  "Selects progressive enhancement or a forced test path.",
  "regions",
  "LiveRegions",
  "Optional",
  "owned regions",
  "Supplies stable polite and assertive elements. Both must belong to the same document.",
  "onDelivery",
  "(result) => void",
  "Optional",
  "undefined",
  "Observes each delivery attempt. Callback failures cannot alter delivery.",
  "result",
  "DeliveryResult",
  "announce returns one result synchronously.",
  "Status",
  "Method",
  "Meaning",
  "notified",
  "aria-notify",
  "ariaNotify returned without throwing",
  "mutated",
  "live-region",
  "Selected stable region text changed",
  "unavailable",
  "none",
  "No usable document or region exists",
  "disposed",
  "none",
  "announce was called after disposal",
  "channel",
  "polite | assertive",
  "Required",
  "n/a",
  "Intent channel selected for this attempt.",
  "announcementId",
  "string",
  "Required",
  "n/a",
  "Stable ID copied from the AnnouncementIntent for diagnostic correlation.",
  "sourceType",
  "RuntimeEvent['type']",
  "Required",
  "n/a",
  "Event type that produced the announcement intent.",
  "at",
  "number",
  "Required",
  "n/a",
  "Injected-clock timestamp copied from the announcement intent.",
  "sourceEventId",
  "string",
  "Optional",
  "undefined",
  "Application event ID when the source event supplied one.",
  "responseId / toolId / interactionId",
  "string",
  "Optional",
  "undefined",
  "Entity IDs copied from the announcement intent when present.",
  "runId / runInstanceId / stepId / stepInstanceId",
  "string",
  "Optional",
  "undefined",
  "Workflow IDs copied from the announcement intent when present.",
  "error",
  "{ name, message }",
  "Optional",
  "undefined",
  "Serializable ariaNotify failure information when fallback was needed.",
  "A notified or mutated result confirms that the library updated the browser. Test with a real screen reader to confirm what it speaks.",
].join("\n");

<span id="signature"></span>

## Signature and import

auto mode attempts ariaNotify when available, then falls back to the selected
polite or assertive live region.

{/* prettier-ignore */}
```typescript
import { createAnnouncer } from "@generative-a11y/dom";

const announcer = createAnnouncer({
  mode: "auto",
  onDelivery(result) {
    console.log(result.status, result.method);
  },
});

const result = announcer.announce(intent);
// Call announcer.dispose() when the owning surface is removed.
```

### Walkthrough

<Steps>

<Step>

### Use progressive delivery

auto keeps the fallback available when ariaNotify is absent or throws.

</Step>

<Step>

### Observe the attempt

DeliveryResult reports the browser action without making a speech claim.

</Step>

<Step>

### Release owned nodes

Dispose removes only live regions created by this announcer.

</Step>

</Steps>

<TypeTable
  type={{
    options: {
      type: <>AnnouncerOptions</>,
      default: <>&#123;&#125;</>,
      description: (
        <>
          Selects document, delivery mode, supplied regions, and diagnostic
          observation.
        </>
      ),
    },
    return: {
      type: <>Announcer</>,
      description: (
        <>
          Return. Owned announcer with announce, getRegions, and dispose
          methods.
        </>
      ),
    },
  }}
/>

<span id="options"></span>

## AnnouncerOptions

Supplying regions transfers neither node ownership nor removal responsibility to
the library.

<TypeTable
  type={{
    document: {
      type: <>Document</>,
      default: <>global document</>,
      description: (
        <>
          Document used to create live regions. Without a document, the
          announcer remains inert and reports unavailable.
        </>
      ),
    },
    mode: {
      type: <>auto &#124; live&#45;region</>,
      default: <>"auto"</>,
      description: <>Selects progressive enhancement or a forced test path.</>,
    },
    regions: {
      type: <>LiveRegions</>,
      default: <>owned regions</>,
      description: (
        <>
          Supplies stable polite and assertive elements. Both must belong to the
          same document.
        </>
      ),
    },
    onDelivery: {
      type: <>(result) &#61;&#62; void</>,
      default: <>undefined</>,
      description: (
        <>
          Observes each delivery attempt. Callback failures cannot alter
          delivery.
        </>
      ),
    },
  }}
/>

<span id="result"></span>

## DeliveryResult

announce returns one result synchronously.

<table aria-label="DeliveryResult table">
  <thead>
    <tr>
      <th>Status</th>
      <th>Method</th>
      <th>Meaning</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>notified</td>
      <td>aria&#45;notify</td>
      <td>ariaNotify returned without throwing</td>
    </tr>
    <tr>
      <td>mutated</td>
      <td>live&#45;region</td>
      <td>Selected stable region text changed</td>
    </tr>
    <tr>
      <td>unavailable</td>
      <td>none</td>
      <td>No usable document or region exists</td>
    </tr>
    <tr>
      <td>disposed</td>
      <td>none</td>
      <td>announce was called after disposal</td>
    </tr>
  </tbody>
</table>

<TypeTable
  type={{
    channel: {
      type: <>polite &#124; assertive</>,
      required: true,
      description: <>Intent channel selected for this attempt.</>,
    },
    announcementId: {
      type: <>string</>,
      required: true,
      description: (
        <>
          Stable ID copied from the AnnouncementIntent for diagnostic
          correlation.
        </>
      ),
    },
    sourceType: {
      type: <>RuntimeEvent&#91;'type'&#93;</>,
      required: true,
      description: <>Event type that produced the announcement intent.</>,
    },
    at: {
      type: <>number</>,
      required: true,
      description: (
        <>Injected&#45;clock timestamp copied from the announcement intent.</>
      ),
    },
    sourceEventId: {
      type: <>string</>,
      default: <>undefined</>,
      description: (
        <>Application event ID when the source event supplied one.</>
      ),
    },
    "responseId / toolId / interactionId": {
      type: <>string</>,
      default: <>undefined</>,
      description: (
        <>Entity IDs copied from the announcement intent when present.</>
      ),
    },
    "runId / runInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Run and attempt IDs copied from the intent when present.</>,
    },
    "stepId / stepInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Step and attempt IDs copied from the intent when present.</>,
    },
    error: {
      type: <>&#123; name, message &#125;</>,
      default: <>undefined</>,
      description: (
        <>
          Serializable ariaNotify failure information when fallback was needed.
        </>
      ),
    },
  }}
/>

<Callout title="Evidence and testing note">

A notified or mutated result confirms that the library updated the browser. Test
with a real screen reader to confirm what it speaks.

</Callout>
