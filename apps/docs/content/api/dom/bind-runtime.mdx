---
title: "bindRuntime"
description:
  "Subscribe an existing core runtime to a Announcer while preserving
  separate runtime and browser ownership."
publicPath: "/api/dom/bind-runtime"
section: "DOM"
keywords: ["bindRuntime", "RuntimeBinding", "dispose"]
related: []
order: 10
---

export const sourceMetadata = {
  publicPath: "/api/dom/bind-runtime",
  section: "DOM",
  related: [],
  order: 10,
};

export const searchableText = [
  "bindRuntime",
  "Subscribe an existing core runtime to a Announcer while preserving separate runtime and browser ownership.",
  "DOM",
  "bindRuntime",
  "RuntimeBinding",
  "dispose",
  "signature",
  "Signature and import",
  "bindRuntime borrows your runtime. Disposing its binding removes only subscription and announcer resources.",
  "typescript",
  'import { createRuntime } from "@generative-a11y/core"; import { bindRuntime } from "@generative-a11y/dom"; const runtime = createRuntime(); const delivery = bindRuntime(runtime, { mode: "auto" }); runtime.dispatch(event); delivery.dispose(); runtime.dispose();',
  "Create the runtime",
  "Your app creates and owns the core runtime.",
  "Connect browser delivery",
  "Connect the DOM binding before your app sends events.",
  "Clean up in order",
  "Dispose the DOM binding before you dispose the runtime.",
  "runtime",
  "Runtime",
  "Required",
  "n/a",
  "Supplies AnnouncementIntent objects while remaining under your app's control.",
  "options",
  "AnnouncerOptions",
  "Optional",
  "{}",
  "Forwarded to the owned DOM announcer.",
  "return",
  "RuntimeBinding",
  "Return",
  "n/a",
  "Provides the announcer and an idempotent dispose function.",
  "ownership",
  "RuntimeBinding",
  "dispose leaves app-supplied runtimes and live regions intact.",
  "announcer",
  "Announcer",
  "Property",
  "n/a",
  "Owned announcer used for every runtime intent.",
  "dispose()",
  "void",
  "Method",
  "n/a",
  "Unsubscribes from the runtime and disposes the owned announcer.",
].join("\n");

<span id="signature"></span>

## Signature and import

bindRuntime borrows your runtime. Disposing its binding removes only
subscription and announcer resources.

{/* prettier-ignore */}
```typescript
import { createRuntime } from "@generative-a11y/core";
import { bindRuntime } from "@generative-a11y/dom";

const runtime = createRuntime();
const delivery = bindRuntime(runtime, { mode: "auto" });

runtime.dispatch(event);

// Call when the chat surface is removed.
export function disposeChat() {
  delivery.dispose();
  runtime.dispose();
}
```

### Walkthrough

<Steps>

<Step>

### Create the runtime

Your app creates and owns the core runtime.

</Step>

<Step>

### Connect browser delivery

Connect the DOM binding before your app sends events.

</Step>

<Step>

### Clean up in order

Dispose the DOM binding before you dispose the runtime.

</Step>

</Steps>

<TypeTable
  type={{
    runtime: {
      type: <>Runtime</>,
      required: true,
      description: (
        <>
          Supplies AnnouncementIntent objects while remaining under your app's
          control.
        </>
      ),
    },
    options: {
      type: <>AnnouncerOptions</>,
      default: <>&#123;&#125;</>,
      description: <>Forwarded to the owned DOM announcer.</>,
    },
    return: {
      type: <>RuntimeBinding</>,
      description: (
        <>Return. Provides the announcer and an idempotent dispose function.</>
      ),
    },
  }}
/>

<span id="ownership"></span>

## RuntimeBinding

dispose leaves app&#45;supplied runtimes and live regions intact.

<TypeTable
  type={{
    announcer: {
      type: <>Announcer</>,
      description: (
        <>Property. Owned announcer used for every runtime intent.</>
      ),
    },
    "dispose()": {
      type: <>void</>,
      description: (
        <>
          Method. Unsubscribes from the runtime and disposes the owned
          announcer.
        </>
      ),
    },
  }}
/>
