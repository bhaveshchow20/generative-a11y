---
title: "@generative-a11y/ag-ui"
description:
  "Turn documented AG-UI AgentSubscriber callbacks into hierarchical run,
  partial step, response, tool, and interaction events."
publicPath: "/api/ag-ui"
section: "AG-UI"
keywords: ["AG-UI", "AgentSubscriber", "adapterInfo"]
related: []
order: 20
---

export const sourceMetadata = {
  publicPath: "/api/ag-ui",
  section: "AG-UI",
  related: [],
  order: 20,
};

export const searchableText = [
  "@generative-a11y/ag-ui",
  "Turn documented AG-UI AgentSubscriber callbacks into hierarchical run, partial step, response, tool, and interaction events.",
  "AG-UI",
  "AG-UI",
  "AgentSubscriber",
  "adapterInfo",
  "exports",
  "Public exports",
  "AG-UI integration subscribes through agent.subscribe(AgentSubscriber).",
  "Export",
  "Kind",
  "Purpose",
  "bindAgent",
  "Function",
  "Create one protocol subscription",
  "adapterInfo",
  "Frozen constant",
  "List supported events and limits",
  "BindAgentOptions",
  "Interface",
  "Options for the binding",
  "AgentBinding",
  "Interface",
  "Cleanup method for the binding",
  "fidelity",
  "Events the adapter can report",
  "AG-UI 0.0.59 reports exact run and delegated subagent identity. Step events expose labels but no stable IDs, making step and hierarchy support partial. Replay and reconnection are partial; retries remain unavailable.",
  "AG-UI integration never uses private observables or guesses events missing from the protocol.",
].join("\n");

<span id="exports"></span>

## Public exports

AG&#45;UI integration subscribes through agent.subscribe(AgentSubscriber).

<table aria-label="Public exports table">
  <thead>
    <tr>
      <th>Export</th>
      <th>Kind</th>
      <th>Purpose</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>bindAgent</td>
      <td>Function</td>
      <td>Create one protocol subscription</td>
    </tr>
    <tr>
      <td>adapterInfo</td>
      <td>Frozen constant</td>
      <td>List supported events and limits</td>
    </tr>
    <tr>
      <td>BindAgentOptions</td>
      <td>Interface</td>
      <td>Options for the binding</td>
    </tr>
    <tr>
      <td>AgentBinding</td>
      <td>Interface</td>
      <td>Cleanup method for the binding</td>
    </tr>
  </tbody>
</table>

<span id="fidelity"></span>

## Events the adapter can report

AG&#45;UI 0.0.59 reports exact run and delegated subagent identity. Step events
expose labels but no stable IDs, making step and hierarchy support partial.
Replay and reconnection are partial because there is no mandatory persistent
cursor; retry support remains unavailable.

<Callout title="Evidence and testing note">

AG&#45;UI integration never uses private observables or guesses events missing
from the protocol.

</Callout>


## Localized announcements

See [catalogs and adapter copy](/docs/localized-announcements) for the typed
`messages` and `copy` options, language ownership, validation,
construction lifetime, and replay requirements. Existing lifecycle evidence
and timing remain unchanged.

import { ApiReference } from "@/components/api-reference";

<ApiReference entry="@generative-a11y/ag-ui" />
