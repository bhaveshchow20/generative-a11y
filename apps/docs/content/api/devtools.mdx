---
title: "@generative-a11y/devtools"
description:
  "Capture redacted runtime diagnostics and inspect them in a browser overlay."
publicPath: "/api/devtools"
section: "Devtools"
keywords: ["createStore", "mountOverlay", "Store", "trace explorer"]
related:
  ["/docs/devtools", "/api/core/diagnostics", "/api/dom/create-announcer"]
order: 22
---

export const sourceMetadata = {
  publicPath: "/api/devtools",
  section: "Devtools",
  related: [
    "/docs/devtools",
    "/api/core/diagnostics",
    "/api/dom/create-announcer",
  ],
  order: 22,
};

export const searchableText = [
  "@generative-a11y/devtools",
  "Capture redacted runtime diagnostics and inspect them in a browser overlay.",
  "createStore",
  "The headless store is framework-neutral and has no browser side effects on import. It subscribes to public runtime diagnostics and owns only its bounded captured history.",
  'import { createStore } from "@generative-a11y/devtools";',
  'const store = createStore({ maxEntries: 250 }); const detach = store.attachRuntime({ id: "support", runtime }); const snapshot = store.getSnapshot(); const trace = store.exportTrace();',
  "detach(); store.dispose();",
  "maxEntries defaults to 250 and must be a positive safe integer. The store borrows public runtime diagnostics; disposing it detaches runtimes without disposing them. Snapshots and trace exports are immutable and content-free.",
  '"createStore(options)" "attachRuntime({ id, runtime, source? })" source "recordDelivery(input)" "exportTrace()" Store maxEntries: 250',
  "Function. Creates an isolated store with bounded records and no attached runtimes.",
  "() => void",
  "Method. Attaches public core diagnostics under a stable ID and returns idempotent detach.",
  "DevtoolsRuntimeSource undefined",
  "Optional attach option. Records the adapter name, documented public evidence, and declared fidelity supplied by the integration.",
  "void",
  "Method. Adds validated content-free DOM delivery evidence correlated by safe IDs.",
  "DevtoolsTraceExportV1",
  "Method. Refreshes runtime snapshots and exports the schema-versioned redacted trace.",
  "Capture controls and ownership",
  "pauseCapture and resumeCapture affect devtools capture only. They do not pause runtime scheduling or browser delivery. clear removes records without detaching runtimes; dispose detaches runtimes and removes subscribers.",
  '"getSnapshot()" "subscribe(listener)" "pauseCapture() / resumeCapture()" "refreshSnapshots()" "clear() / dispose()" DevtoolsSnapshot',
  "Method. Returns a cached immutable view until captured state changes.",
  "() => void",
  "Method. Observes store changes with an idempotent unsubscribe function.",
  "void",
  "Method. Controls diagnostic retention without changing the observed runtimes.",
  "void",
  "Method. Requests fresh content-free snapshots from attached runtimes without changing their state.",
  "void",
  "Method. Clears captured state or releases all store-owned resources.",
  "Snapshot and trace contracts",
  "Devtools snapshots and V1 exports contain redacted records, runtime snapshots, and declared adapter sources. The bounded record list reports how much older data it dropped.",
  "droppedCount runtimeSourceId records runtimeSnapshots runtimeSources number 0",
  "Snapshot and export. Counts records removed from the ring buffer since the last clear.",
  "string undefined",
  "Optional record field. Connects a record to an immutable adapter evidence revision without copying that metadata into each record.",
  "readonly DevtoolsRecord[] []",
  "Snapshot and export. Contains redacted events, decisions, optional DOM delivery results, workflow identity, hierarchy, and attempt replacement fields in capture order.",
  "Readonly<Record<string, RuntimeDiagnosticSnapshotV1>> {}",
  "Snapshot and export. Stores the latest content-free core snapshot for each attached runtime.",
  "Readonly<Record<string, DevtoolsRuntimeSource>> {}",
  "Snapshot and export. Stores immutable adapter evidence revisions still referenced by retained records.",
  "Workflow record correlation",
  "Workflow records retain runId, runInstanceId, stepId, and stepInstanceId. Parent fields preserve only hierarchy supplied by the source. nextRunInstanceId and nextStepInstanceId connect a retry record to its replacement attempt. The trace explorer uses these exact attempt keys so concurrent or replaced attempts are not merged by label.",
  '"runId / runInstanceId / nextRunInstanceId" "stepId / stepInstanceId / nextStepInstanceId" "parentRunId / parentRunInstanceId" "parentStepId / parentStepInstanceId" "parentToolId / parentResponseId" string undefined Logical run, current attempt, and replacement attempt. string undefined Logical step, current attempt, and replacement attempt. string undefined Explicit parent run correlation. string undefined Explicit parent step correlation. string undefined Explicit delegation and response ownership.',
  "Adapter fidelity",
  "DevtoolsRuntimeSource stores an integration's declared evidence and fidelity. Workflow fields cover runs, steps, hierarchy, tools, interactions, replay, reconnection, retries, interruption, connection, and custom events. Devtools does not inspect framework internals or fill in evidence the adapter lacks.",
  "mountOverlay",
  "Import the browser helper from @generative-a11y/devtools/overlay. It mounts one Shadow DOM host after an explicit call, starts collapsed, and restores prior focus when closed.",
  'import { mountOverlay } from "@generative-a11y/devtools/overlay";',
  "const overlay = mountOverlay({ store }); // Call overlay.dispose() when removing the workbench.",
  "The overlay reads your store without attaching runtimes itself. Importing the module creates no browser UI or global shortcuts. dispose() removes the workbench and its host.",
  'store document copyText host "dispose()" Store',
  "Supplies the captured records and control methods displayed by the overlay.",
  "Document global document Selects the document that receives the Shadow DOM host. (value) => void | Promise<void> Clipboard API",
  "Overrides trace copying for hosts that provide their own clipboard bridge.",
  "HTMLElement",
  "Return field. Exposes the mounted custom element for inspection or host-controlled placement.",
  "void",
  "Return method. Unmounts the workbench and removes its host. Repeated calls are safe.",
  "Captured runtime and browser evidence does not prove that assistive technology spoke an announcement.",
  "Localized announcements",
  "See catalogs and adapter copy /docs/localized-announcements for the typed messages and copy options, language ownership, validation, construction lifetime, and replay requirements. Existing lifecycle evidence and timing remain unchanged.",
].join("\n");

<span id="store"></span>

## createStore

The headless store is framework&#45;neutral and has no browser side effects on
import. It subscribes to public runtime diagnostics and owns only its bounded
captured history.

{/* prettier-ignore */}
```typescript title="Capture runtime diagnostics"
import { createStore } from "@generative-a11y/devtools";

const store = createStore({ maxEntries: 250 });
const detach = store.attachRuntime({ id: "support", runtime });
const snapshot = store.getSnapshot();
const trace = store.exportTrace();

detach();
store.dispose();
```

`maxEntries` defaults to 250 and must be a positive safe integer. The store
borrows public runtime diagnostics; disposing it detaches runtimes without
disposing them. Snapshots and trace exports are immutable and content-free.

<TypeTable
  type={{
    "createStore(options)": {
      type: <>Store</>,
      default: <>maxEntries: 250</>,
      description: (
        <>
          Function. Creates an isolated store with bounded records and no
          attached runtimes.
        </>
      ),
    },
    "attachRuntime({ id, runtime, source? })": {
      type: <>() &#61;&#62; void</>,
      description: (
        <>
          Method. Attaches public core diagnostics under a stable ID and returns
          idempotent detach.
        </>
      ),
    },
    source: {
      type: <>DevtoolsRuntimeSource</>,
      default: <>undefined</>,
      description: (
        <>
          Optional attach option. Records the adapter name, documented public
          evidence, and declared fidelity supplied by the integration.
        </>
      ),
    },
    "recordDelivery(input)": {
      type: <>void</>,
      description: (
        <>
          Method. Adds validated content&#45;free DOM delivery evidence
          correlated by safe IDs.
        </>
      ),
    },
    "exportTrace()": {
      type: <>DevtoolsTraceExportV1</>,
      description: (
        <>
          Method. Refreshes runtime snapshots and exports the
          schema&#45;versioned redacted trace.
        </>
      ),
    },
  }}
/>

<span id="capture&#45;controls"></span>

<span id="walkthrough"></span>

<span id="bound-retained-records"></span>

<span id="attach-public-diagnostics"></span>

<span id="read-or-export"></span>

<span id="release-capture"></span>

## Capture controls and ownership

pauseCapture and resumeCapture affect devtools capture only. They do not pause
runtime scheduling or browser delivery. clear removes records without detaching
runtimes; dispose detaches runtimes and removes subscribers.

<TypeTable
  type={{
    "getSnapshot()": {
      type: <>DevtoolsSnapshot</>,
      description: (
        <>
          Method. Returns a cached immutable view until captured state changes.
        </>
      ),
    },
    "subscribe(listener)": {
      type: <>() &#61;&#62; void</>,
      description: (
        <>
          Method. Observes store changes with an idempotent unsubscribe
          function.
        </>
      ),
    },
    "pauseCapture() / resumeCapture()": {
      type: <>void</>,
      description: (
        <>
          Method. Controls diagnostic retention without changing the observed
          runtimes.
        </>
      ),
    },
    "refreshSnapshots()": {
      type: <>void</>,
      description: (
        <>
          Method. Requests fresh content&#45;free snapshots from attached
          runtimes without changing their state.
        </>
      ),
    },
    "clear() / dispose()": {
      type: <>void</>,
      description: (
        <>
          Method. Clears captured state or releases all store&#45;owned
          resources.
        </>
      ),
    },
  }}
/>

<span id="trace&#45;contracts"></span>

## Snapshot and trace contracts

Devtools snapshots and V1 exports contain redacted records, runtime snapshots,
and declared adapter sources. The bounded record list reports how much older
data it dropped.

<TypeTable
  type={{
    droppedCount: {
      type: <>number</>,
      default: <>0</>,
      description: (
        <>
          Snapshot and export. Counts records removed from the ring buffer since
          the last clear.
        </>
      ),
    },
    runtimeSourceId: {
      type: <>string</>,
      default: <>undefined</>,
      description: (
        <>
          Optional record field. Connects a record to an immutable adapter
          evidence revision without copying that metadata into each record.
        </>
      ),
    },
    records: {
      type: <>readonly DevtoolsRecord&#91;&#93;</>,
      default: <>&#91;&#93;</>,
      description: (
        <>
          Snapshot and export. Contains redacted events, decisions, optional DOM
          delivery results, workflow identity, hierarchy, and attempt
          replacement fields in capture order.
        </>
      ),
    },
    runtimeSnapshots: {
      type: (
        <>Readonly&lt;Record&lt;string, RuntimeDiagnosticSnapshotV1&#62;&#62;</>
      ),
      default: <>&#123;&#125;</>,
      description: (
        <>
          Snapshot and export. Stores the latest content&#45;free core snapshot
          for each attached runtime.
        </>
      ),
    },
    runtimeSources: {
      type: <>Readonly&lt;Record&lt;string, DevtoolsRuntimeSource&#62;&#62;</>,
      default: <>&#123;&#125;</>,
      description: (
        <>
          Snapshot and export. Stores immutable adapter evidence revisions still
          referenced by retained records.
        </>
      ),
    },
  }}
/>

<span id="workflow&#45;records"></span>

## Workflow record correlation

Workflow records retain `runId`, `runInstanceId`, `stepId`, and
`stepInstanceId`. Parent fields preserve only hierarchy supplied by the source.
`nextRunInstanceId` and `nextStepInstanceId` connect a retry record to its
replacement attempt. The trace explorer uses these exact attempt keys so
concurrent or replaced attempts are not merged by label.

<TypeTable
  type={{
    "runId / runInstanceId / nextRunInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Logical run, current attempt, and replacement attempt.</>,
    },
    "stepId / stepInstanceId / nextStepInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Logical step, current attempt, and replacement attempt.</>,
    },
    "parentRunId / parentRunInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Explicit parent run correlation.</>,
    },
    "parentStepId / parentStepInstanceId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Explicit parent step correlation.</>,
    },
    "parentToolId / parentResponseId": {
      type: <>string</>,
      default: <>undefined</>,
      description: <>Explicit delegation and response ownership.</>,
    },
  }}
/>

### Adapter fidelity

`DevtoolsRuntimeSource` stores an integration's declared evidence and fidelity.
Workflow fields cover runs, steps, hierarchy, tools, interactions, replay,
reconnection, retries, interruption, connection, and custom events. Devtools
does not inspect framework internals or fill in evidence the adapter lacks.

<span id="overlay"></span>

## mountOverlay

Import the browser helper from @generative&#45;a11y/devtools/overlay. It mounts
one Shadow DOM host after an explicit call, starts collapsed, and restores prior
focus when closed.

{/* prettier-ignore */}
```typescript title="Mount the trace explorer"
import { mountOverlay } from "@generative-a11y/devtools/overlay";

const overlay = mountOverlay({ store });
// Call overlay.dispose() when removing the workbench.
```

The overlay reads your store without attaching runtimes itself. Importing the
module creates no browser UI or global shortcuts. `dispose()` removes the
workbench and its host.

<TypeTable
  type={{
    store: {
      type: <>Store</>,
      required: true,
      description: (
        <>
          Supplies the captured records and control methods displayed by the
          overlay.
        </>
      ),
    },
    document: {
      type: <>Document</>,
      default: <>global document</>,
      description: <>Selects the document that receives the Shadow DOM host.</>,
    },
    copyText: {
      type: <>(value) &#61;&#62; void &#124; Promise&lt;void&#62;</>,
      default: <>Clipboard API</>,
      description: (
        <>
          Overrides trace copying for hosts that provide their own clipboard
          bridge.
        </>
      ),
    },
    host: {
      type: <>HTMLElement</>,
      description: (
        <>
          Return field. Exposes the mounted custom element for inspection or
          host&#45;controlled placement.
        </>
      ),
    },
    "dispose()": {
      type: <>void</>,
      description: (
        <>
          Return method. Unmounts the workbench and removes its host. Repeated
          calls are safe.
        </>
      ),
    },
  }}
/>

<Callout title="Evidence and testing note">

Captured runtime and browser evidence does not prove that assistive technology
spoke an announcement.

</Callout>

<span id="walkthrough-1"></span>

<span id="pass-the-store"></span>

<span id="mount-explicitly"></span>

<span id="dispose-the-host"></span>

## Localized announcements

See [catalogs and adapter copy](/docs/localized-announcements) for the typed
`messages` and `copy` options, language ownership, validation, construction
lifetime, and replay requirements. Existing lifecycle evidence and timing remain
unchanged.

import { ApiReference } from "@/components/api-reference";

<ApiReference entry="@generative-a11y/devtools" />

<ApiReference entry="@generative-a11y/devtools/overlay" />
