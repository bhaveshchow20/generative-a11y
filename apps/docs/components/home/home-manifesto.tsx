export function HomeManifesto() {
  return (
    <section className="home-manifesto" aria-labelledby="home-manifesto-title">
      <h2 id="home-manifesto-title" className="sr-only">The interface stays yours</h2>
      <p>
        generative-a11y adds <em>accessible behavior</em> to the AI interface
        you already have without replacing its UI or stealing focus. If
        lifecycle evidence is incomplete, <em>it says so</em>.
      </p>
    </section>
  );
}
